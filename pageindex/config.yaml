# ==============================================================================
# PageIndex Model Configuration (TARGET 1.4)
# ==============================================================================

# Default Provider Selection
provider: "ollama"  # Options: "ollama", "openai", "hybrid" (default: local Ollama)

# OpenAI Model Configuration (optional fallback/compatibility)
model: "gpt-4o-2024-11-20"  # OpenAI model (128K context window)

# Ollama Model Configuration
# Default local model for fully private inference (no external API required)
ollama_model: "mistral24b-16k"  # Default local model (overridden by OLLAMA_MODEL env var)

# Alternative Ollama Models (can be set via OLLAMA_MODEL env var):
# - mistral24b-16k    → 24B, 16k context window, production-tuned (default)
# - mistral24b-prod   → 24B, constrained output profile (legacy)
# - mistral-small:24b → 24B, 32k context (base)
# - qwen2.5:14b       → 14B, 128k context window
# - mistral:7b        → 7B, 8k context
# - phi3:3.8b         → 3.8B, lightweight and fast
# - llama3:8b         → 8B, 8k context
# - gemma:2b          → 2B, fast but less capable

# Model Capabilities
model_config:
  openai:
    context_window: 128000
    supports_json_mode: true
    supports_streaming: true
  
  ollama:
    context_window: 16384  # mistral24b-16k context window (16k)
    supports_json_mode: false  # Most Ollama models
    supports_streaming: true

# Document Processing Configuration
toc_check_page_num: 20
max_page_num_each_node: 10
max_token_num_each_node: 20000
if_add_node_id: "yes"
if_add_node_summary: "yes"
if_add_doc_description: "no"
if_add_node_text: "no"